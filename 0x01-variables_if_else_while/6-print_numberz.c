#include <stdlib.h>
#include <time.h>
#include <stduio>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int alpha;

	alpha = 78;
	while (alpha < 88)
	{
		putchar(alpha);
		alpha++;
	}
	putchar('\n');
	return (0);
}
